name: Export v95-work source artifact

on:
  push:
    branches: [v95-work]
    paths:
      - '.trigger_export_v95_source'

permissions:
  contents: read

jobs:
  export:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: v95-work
      - uses: actions/upload-artifact@v4
        with:
          name: v95-work-source
          path: working_source
          if-no-files-found: error
          retention-days: 1
